version: "3"
services:
  mongo:
    container_name: mongo-container
    image: mongo:8.0.9
    restart: always
    volumes:
      - ./data/db:/data/db
    ports:
      - "27017:27017"
  api:
    container_name: api-container
    image: clean-node-api
    build: .
    command: npm start
    volumes:
      - ./dist:/usr/src/api/dist
    ports:
      - "5050:5050"
    depends_on:
      - mongo
